import React from "react";
import { useLazyQuery, useQuery } from "@apollo/client";
import { GET_VIDEOS } from "@/graphql-client/gql-news";
const VideoPage = () => {
  return <React.Fragment>VideoPage</React.Fragment>;
};

export default VideoPage;
